# Textures

Reserved texture slots:

- `nebula-dust.png` for low-contrast background dust.
- `star-glow.png` for bright star sprites.
- `wormhole-energy.png` for tunnel particles.

The current native renderer generates these effects procedurally to keep the build dependency-free.
