# Fonts

The native renderer uses Segoe UI from Windows so the executable has no font installation dependency.

A future GPU renderer can place bundled `.ttf` files in this folder and load them explicitly.
